

<div class="middle-box text-center loginscreen animated fadeInDown">
  <div>
      <h3>비밀번호를 잊어버리셨나요?</h3>
      <p>가입하신 이메일을 입력하면<br> 패스워드 재설정 메일을 보내드립니다.</p>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, class: "m-t") do |f| %>
        <%#= devise_error_messages! %>    
        <%= render 'layouts/error_messages', object: f.object %> 
        <div class="form-group">
          <%#= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: "E-mail" %>
        </div>
        <%= f.submit "패스워드 설정 메일 보내기", class: "btn btn-primary block full-width m-b"%>
      <% end %>
  </div>
  <%= render "devise/shared/links" %>

</div>