<%= form_for(@space) do |f| %>
  <% if @space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@space.errors.count, "error") %> prohibited this space from being saved:</h2>
      <ul>
      <% @space.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" col="3">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </div>
  <div class="field">
    <%= f.label :map %><br>
    <%= f.text_area :map %>
  </div>
  <div class="field">
    <%= f.label :service_time %><br>
    <%= select_date Date.today, prefix: :service_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
